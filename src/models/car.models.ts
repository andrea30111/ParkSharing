export class Car{
    plate: string;
    type: number;
    color: string;
    user: string

    constructor(plate: string, type: number, color: string, user: string){
        this.plate = plate;
        this.type = type;
        this.color = color;
        this.user = user;
    }
}
